# Prevent accidental npm/yarn installs — use pnpm only
engine-strict=true

# Hoisting settings — hoist all packages to root node_modules for compatibility
shamefully-hoist=true

# Auto-install peers
auto-install-peers=true

